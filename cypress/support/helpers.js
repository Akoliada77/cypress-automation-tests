function getRandomInt(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min) + min);    
};

function getSomeRandomInt(quantity, min, max) {
    let firstNumber = 0;
}

export { getRandomInt } ;